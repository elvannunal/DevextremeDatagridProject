export interface BaseEmployee {
  firstName: string;
  lastName: string;
  title: string;
  position: string;
  birthDate: string;
  hireDate: string;
  notes: string;
  address: string;
  countryCode: string;
  definition?: string;
}
